{% extends 'base.html.twig' %}

{% block title %}Editar juego | TournamentFlow{% endblock %}

{% block body %}
    <div class="card">
        <h1 class="title">Editar juego</h1>
        <p class="subtitle">Ajusta nombre o imagen.</p>

        {{ include('game/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="actions-row">
            <a class="button ghost" href="{{ path('app_game_index') }}">Volver</a>
            {{ include('game/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
